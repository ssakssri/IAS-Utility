# NextAuth
NEXTAUTH_SECRET="generate-with: openssl rand -base64 32"
NEXTAUTH_URL="http://localhost:3000"

# Database (SQLite for dev, PostgreSQL for prod)
DATABASE_URL="file:./dev.db"

# Tenant Config 암호화 키 (32바이트 hex)
# 생성: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
CONFIG_ENCRYPTION_KEY="your-64-char-hex-key-here"

# Admin 초기 계정 (seed용)
ADMIN_EMAIL="admin@company.com"
ADMIN_PASSWORD="AdminP@ssw0rd!"

# ENV 폴백 - DB에 TenantConfig 없을 때만 사용 (선택)
# SAP_IAS_TENANT_URL="https://xxxxx.accounts.ondemand.com"
# SAP_IAS_CLIENT_ID="scim-client-id"
# SAP_IAS_CLIENT_SECRET="scim-client-secret"
